void setup() {
  // put your setup code here, to run once:
  //Handy als Client, Board als Server
uip_ipadd(&addr,<span style="color: #ff0000;">192.168.0.1</span>);
c=uip_udp_new(&addr,HTONS(<span style="color:#ff0000;">0</span>))
}

void loop() {
  // put your main code here, to run repeatedly:

}
